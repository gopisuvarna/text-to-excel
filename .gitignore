# Python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/

# Secrets — never commit
.env
frontend/.env.production
frontend/.env.local

# Dev env file is safe to commit (no secrets)
# frontend/.env.development  ← intentionally NOT ignored

# React/Vite
frontend/node_modules/
frontend/dist/

# Runtime output — not stored in git (Render uses /tmp)
output/
uploads/

# Editor / OS
.vscode/
.DS_Store
*.log
