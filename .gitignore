# Python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/

# Secrets — never commit
.env
credentials.json
frontend/.env.local

# .env.production is NOT secret (public Render URL) — intentionally committed
# so Vercel picks it up during build without needing a dashboard variable

# Dev env file is safe to commit (no secrets)
frontend/.env.development  

# React/Vite
frontend/node_modules/
frontend/dist/

# Runtime output — not stored in git (Render uses /tmp)
output/
uploads/
credentials.json
# Editor / OS
.vscode/
.DS_Store
*.log
